package clases.enums.entities;

/**
 * Los Enum (enumeraciones) nos permiten crear listas de valores definidos, es decir, podemos escribir una serie de palabras, en MAYUSCULAS y separadas por comas
 * Para despues utilizar estos valores especificos.
 *
 * <P>
 *     Si creamos una variable o una propiedad de @Type
 * </P>
 */
public enum FileType {
    IMAGE, DOCUMENT, VIDEO, SOUND, UNKNOWN
}
